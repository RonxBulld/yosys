read_verilog vbuf_test.v

# 显示所有VBUF实例
select t:VBUF
select -list

# 分析连接关系 - 方法1
select -clear
select t:VBUF
select %x:+[O]
select %x:+[I] t:VBUF
select -list

# 分析连接关系 - 方法2
select -clear
select t:VBUF
select %x:+[O]
select %x:+[I] t:VBUF
select -list